# Cortexa AI Agent Platform — Consolidated v0.9.6.1 Baseline

This snapshot consolidates the complete Phase 9.4 repository with:

- Phase 9.5.1 durable agent telemetry
- Phase 9.5.1 validation scanner fix
- Phase 9.5.2 failure classification and retry policy
- Phase 9.6 browser-based Agent Run launcher
- Phase 9.6.1 session refresh, live polling, and retry timeline reliability fixes

The archive intentionally excludes `.git`, `.env`, dependency caches, build output, and Docker volumes. Preserve the existing local `.git` directory and runtime data when replacing source files.
