{
  "name": "cortexa-ai-agent-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Cortexa AI Agent Platform — monorepo root (Phase 1 application foundation).",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "lint": "npm run lint --workspace=@cortexa/frontend",
    "typecheck": "npm run typecheck --workspace=@cortexa/frontend",
    "test": "npm run test:run --workspace=@cortexa/frontend",
    "build": "npm run build --workspace=@cortexa/frontend"
  }
}
