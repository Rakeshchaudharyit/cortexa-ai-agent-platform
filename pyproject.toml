[project]
name = "cortexa-ai-agent-platform"
version = "0.1.0"
description = "Cortexa AI Knowledge Platform — enterprise RAG, knowledge governance, and AI quality monorepo."
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = { text = "Proprietary" }
authors = [{ name = "Cortexa Maintainers" }]

# Root metadata only. Backend package lives in backend/pyproject.toml.
dependencies = []

[build-system]
requires = ["setuptools>=75.0.0", "wheel"]
build-backend = "setuptools.build_meta"
